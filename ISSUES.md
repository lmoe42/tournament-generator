# Tournament Generator — Verified Issue Resolution

Generated by prior audit: 2026-03-01
Verified and resolved: 2026-05-30

The prior backlog was checked against the codebase before implementation. All concrete items were relevant in the pre-fix codebase and have been addressed in this change set.

## Resolution Summary

| #   | Status    | Resolution                                                                                                     |
| --- | --------- | -------------------------------------------------------------------------------------------------------------- |
| 1   | Fixed     | `saveTournament` now upserts instead of dropping new tournaments.                                              |
| 2   | Fixed     | `getTournament` now returns the matching tournament.                                                           |
| 3   | Fixed     | Participant updates refresh the sorted table state.                                                            |
| 4   | Fixed     | Participant additions use immutable arrays.                                                                    |
| 5   | Fixed     | Event result scoring and input updates clone nested state before writing.                                      |
| 6   | Fixed     | Table sorting handles missing event and overall scores safely.                                                 |
| 7   | Fixed     | Sorting uses copied participant arrays instead of mutating state.                                              |
| 8   | Fixed     | All tournament localStorage parsing is centralized and guarded.                                                |
| 9   | Fixed     | Participant deletion uses modal state and parent callbacks, not prop mutation.                                 |
| 10  | Fixed     | Invalid or empty performances are normalized to zero.                                                          |
| 11  | Fixed     | Empty event result objects return an empty result instead of crashing.                                         |
| 12  | Fixed     | Duplicate tournament names are rejected with an inline MUI error.                                              |
| 13  | Fixed     | Components now use `logic/persistence` instead of direct localStorage access.                                  |
| 14  | Fixed     | Participant and event modals sync state when opened.                                                           |
| 15  | Fixed     | Result clearing and tournament deletion update React state without reloads.                                    |
| 16  | Fixed     | Tournament lists initialize from persistence state instead of parsing on every render.                         |
| 17  | Fixed     | Tournament route navigation encodes names and route readers decode them.                                       |
| 18  | Fixed     | Native `confirm` and `alert` calls were replaced with MUI UI.                                                  |
| 19  | Fixed     | Unknown tournament routes render an actionable not-found state.                                                |
| 20  | Fixed     | Custom events can be configured as higher- or lower-is-better.                                                 |
| 21  | Fixed     | New tournament modal state resets on close and after creation.                                                 |
| 22  | Fixed     | `persistance.ts` was renamed to `persistence.ts`; imports were updated.                                        |
| 23  | Fixed     | `makeStyles` usage and the unused `@mui/styles` package were removed.                                          |
| 24  | Fixed     | Existing tournaments now use MUI `Grid2`.                                                                      |
| 25  | Fixed     | List renders now use stable keys.                                                                              |
| 26  | Fixed     | React type packages were updated to React 19 versions.                                                         |
| 27  | Fixed     | `@types/jest` was removed from the Vitest project.                                                             |
| 28  | Fixed     | Tooling packages were moved from `dependencies` to `devDependencies`.                                          |
| 29  | Fixed     | The accidental `add` package was removed.                                                                      |
| 30  | Fixed     | `TournamentOverview` uses `TournamentTypes` for its `type` prop.                                               |
| 31  | Fixed     | Existing tournament list state uses the shared `Tournament` type.                                              |
| 32  | Fixed     | Vitest now uses the `tsconfigPaths` plugin.                                                                    |
| 33  | Fixed     | Vitest now runs with `jsdom` for React-capable tests.                                                          |
| 34  | Addressed | Regression coverage was added for persistence and scoring edge cases; jsdom is configured for component tests. |
| 35  | Fixed     | The redundant new-tournament handler wrapper was removed.                                                      |
| 36  | Fixed     | The duplicated landing-page gradient was removed.                                                              |
| 37  | Fixed     | Removed with the `makeStyles` refactor.                                                                        |
| 38  | Fixed     | `TournamentStrongman` now uses a consistent `useState` style.                                                  |
| 39  | Fixed     | Internal imports now use path aliases.                                                                         |
| 40  | Fixed     | The stale test file-path comment was removed.                                                                  |
| 41  | Fixed     | `getEventResult` tests are nested under the tournament test suite.                                             |
| 42  | Fixed     | TypeScript module resolution now uses `Bundler`.                                                               |
| 43  | Fixed     | TypeScript was updated to 5.x.                                                                                 |
| 44  | Fixed     | MUI modals now include labelled and described accessibility metadata.                                          |
| 45  | Fixed     | Tournament cards now use `CardActionArea` for keyboard access.                                                 |
| 46  | Fixed     | Trophy image alt text now describes the image.                                                                 |

## Verification

- Added regression tests in `src/tests/persistence.test.ts`.
- Expanded scoring tests in `src/tests/resultCalculation.test.ts`.
- Verified with `npm test -- --run`.
- Verified production build with `npm run build`.
- Smoke-tested the main browser flows on `http://127.0.0.1:5173/`.
